import type { ReactNode } from "react";

/** 3-tier theme wrapper + persistent medical disclaimer. */
export function DashboardLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
