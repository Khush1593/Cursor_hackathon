import { Injectable } from '@nestjs/common';

/**
 * HTTP client for the Python FastAPI triage service.
 * Stub only — POST /triage + USE_AI_STUB path to be implemented.
 */
@Injectable()
export class AiClient {
  // TODO: POST TriageRequest → AuraResponse (or stub when USE_AI_STUB=true)
}
